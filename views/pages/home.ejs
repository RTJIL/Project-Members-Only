<div class="home-container">
  <!-- if clause if logged in show content without log in message -->
  <% if (!isAuthenticated) { %>
  <div class="recommendation-container">
    <h2>⛔U shall not pass, if not <a href="/sign-in">signed in</a></h2>
  </div>
  <% } %>

  <!-- if clause if logged in show content without log in message -->
  <% if (isAuthenticated) { %>
  <div class="messages-container">
    <% if (member) { %>
    <div class="vip-badge"><p>💎 VIP Member</p><a href="/delete-vip">Sell Vip</a></div>
    <% } else { %>
    <a href="/get-vip" class="vip-btn">Become a VIP</a>
    <% } %> <% posts.forEach(post => { %>
    <div class="message">
      <% if (member) { %>
      <h2><%= post ? post.username : "Unknown" %></h2>
      <% } else { %>
      <h2>****</h2>
      <% } %>
      <h4><%= post.title %></h4>
      <p><%= post.message %></p>
    </div>
    <% } ) %>
  </div>
  <% } %>
</div>
